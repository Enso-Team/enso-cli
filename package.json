{
  "name": "@enso-app/cli",
  "version": "0.7.1",
  "description": "Local command line interface for the Enso Mac app.",
  "type": "module",
  "bin": {
    "enso": "dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist",
    "skills"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsx src/index.ts",
    "dev:install": "npm run build && npm link",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "proper-lockfile": "^4.1.2",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "@types/proper-lockfile": "^4.1.4",
    "tsup": "^8.1.0",
    "tsx": "^4.16.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Enso-Team/enso-cli.git"
  },
  "homepage": "https://github.com/Enso-Team/enso-cli#readme",
  "bugs": {
    "url": "https://github.com/Enso-Team/enso-cli/issues"
  }
}
